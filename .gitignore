# Dependencies
node_modules/

# Environment variables (secrets)
.env
.env.local
.env.production

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Médias du site : VERSIONNÉS dans git (images salon/barbers + vidéos/réalisations).
# Leur perte — gitignorés + incident iCloud 2026-05 — avait cassé le site (2 fois)
# après des déploiements depuis un disque incomplet. On les garde dans git.

# Cloudflare Wrangler cache
.wrangler/

# Git worktrees (isolated workspaces for feature dev)
.worktrees/

# Build artifacts
dashboard/dist/
dashboard-grenoble/dist/
*.tmp
*.bak

# Grenoble old Timify page (kept as backup)
pages/grenoble/reserver-timify.html

# IDE
.vscode/
.idea/

# Claude Code local settings
.claude/
.gstack/
